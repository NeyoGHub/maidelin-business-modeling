---
name: maidelin-business-modeling
description: Use when 需要查询/分析麦得邻 S6 商超系统业务数据（销售/退货/费用/库存/回款/客户/商品/业务员），或用自然语言查数、做客户价值分析与客户分层。加载后能按本 Skill 的数据字典与指标口径从 S6 取数，输出可计算画像：客户ABC分级(利润20/50/30)、活跃度四档、客户利润(含商品成本)、真实退货(报损)、回款周期、返利、客情、商品分级、业务员考核。分析麦得邻客户价值、量化客户健康度时使用。
---

# 麦得邻客户业务建模量化（商贸流通/经销商行业通用方法论）

> 本 Skill 是**平台无关**的通用知识：任何 Agent（Hermes / Claude / Cursor / OpenCode 等，支持 agentskills.io 的 SKILL.md 即可）加载后，都能用它连接 S6 数据库、按统一指标口径查询与分析。

## 用途

把商贸流通/经销商公司的客户业务量化成**可计算的模型**，用于客户 ABC 分级、客户价值评估、AI 驱动业务员（该跑哪家、精力放哪）。所有指标口径统一，分析/报告须附带指标口径说明与数据溯源（读者可登录 S6 界面反验）。

## 新需求数据探索标准流程（无现成口径时）

当业务方问到"S6 里存在但以前没人查过"的新数据（无现成表/字段/口径模板）时：

1. **走数据通道探索**：数据必然落在 S6 某张表某字段，用 execute_query 翻主档、翻表结构（系统表/字典表）定位，全程不离开统一数据通道，不另开直连后门。
2. **定口径**：按业务逻辑定义算法（哪些状态算数 zt<>-1？时间范围？含不含未审核？哪些仓库/渠道？），写 SQL 走通道跑。
3. **反验**：跑出的数字与 S6 界面或另一条交叉 SQL 核对，确认不是瞎算，才交付。
4. **交付即带口径**：来源表/字段/筛选/算法说明，对方能在 S6 界面反验。
5. **沉淀技能**：验证可行后，把表名/字段/口径/现成 SQL 模板写回本 SKILL（新增条目或 references），下次同类数据直接套用——"越查越懂"。

**边界**：S6 里压根没有的数据（系统外）→ 明说查不了，绝不编数字；需接新数据源 → 报业务负责人定夺后再并入同一数据通道，不自行另起炉灶。

## 自然语言查 S6（通用执行流程）

任何 Agent 可用自然语言查询 S6 业务数据。执行流程：

1. **解析意图** → 指标 / 对象 / 时间范围（如"上个月销售前10的流通客户及毛利"）
2. **查口径** → 用下方各维度指标定义确定取数规则（客户利润必含成本、真实退货按报损仓、活跃度取全历史最近开单）
3. **取/拼 SQL** → 优先用 `scripts/` 模板（01 客户/02 回款/03 商品/04 业务员），不够按数据源表拼 SQL（**多指标各自 CTE 聚合再 JOIN，防笛卡尔积**）
4. **执行** → 连 S6（方式见 `references/S6-QUERY.md`；只读；SQL Server 2014 语法限制）
5. **返回** → 用数据字典解释字段；数字附指标口径说明 + 可在 S6 核对的数据源（可反验）

## 核心数据源（S6 数据字典）

| 数据 | 表 | 关键字段 |
|---|---|---|
| 直营销售单 | `zyxsd` | xsdh(YD-单号) / khbh / **xsrq** / xsje / **zt(1=已审核,0=未审核,-1=作废)** / shsj(审核) / ywybh / jdrq(录单) / apkwsj(同步) |
| 销售单明细 | `zyxsdmx` | xsdh / cpbh / **xssl(数量)** / xsje / **jhj(进货价,商品成本用它)** |
| 销售发货单 | `xsfhd` | **fhdh(XF-单号)** / **zyxsdh(关联销售单)** / shsj |
| 销售收款单 | `xsskd` | skdh / khbh / skje / skrq / shsj / zt |
| 收款核销明细 | `xsskd_hxmx` | skdh / **djhm(被核销单号)** / djlx(**f0=销售**) |
| 销售退货单 | `xsthd` | khbh / thrq / thje / **thck(去向仓库)** / zt IN(1,2) |
| 退货单明细 | `xsthdmx` | thdh / cpbh / thsl / **cbje(成本金额)** / cbdj / thje |
| 销售费用单 | `xsfy` | khbh / fyje（**已含券/满减/返现**）/ fyrq / zt |
| 优惠券明细 | `zdy_xsfy_yhqje` | fydh / cpbh / yhqje（用券分析列，不重复扣） |
| 客户档案 | `kh` | khbh / dwmc / ywybh / **sfyx** / **khlx(1=渠道主体,2=终端)** / **sszgs(渠道根)** / sjkh(上级) |
| 业务员 | `ywy` | ywybh / xm(姓名) / sfywy / **bmdm** |
| 当前库存 | `cpkc` | ckbh(仓库) / cpbh / cpsl / cbdj |
| 仓库字典 | `ck` | ckbh / ckmc（主仓 / 报损仓 / 退货仓 / 返厂仓等） |
| 批次库存 | `bi_Stmt_SKU_InvBatch` | **idWH(仓库)** / MFG / DTE / **LifeLine** / a_Real |
| 客户应收期末余额 | `TF_getKhyskQmzys(起,止,khbh)` | qmzysk=期末应收（另含 qczysk 期初/qjyis 期间已收） | 查"客户欠款/还差多少钱/应收余额"用它（官方口径，与 S6 应收款界面一致） |
| 客户往来对账单 | `TF_getDjmxForKhYskDtb(khbh,起,止)` | 期初/票前调整单/销售发货单/销售收款单/票前退货单/票前销售费用/期末；期末行 qjje=余额 | S6 客户应收款对账界面同源，可逐笔反验 |
| 应收账龄(滚动窗口) | `bi_Stmt_ARB` | Receivable / WO_Exp / WO_ExpD / d_dbTerm | 仅含近约30天开单未结，勿当全量应收余额 |
| 客户应收动态流水 | `khinfoqmzysk_log` | xsfhd+/xsskd-/xstzd调整/xsthd退货/xsfy费用；czlx 1=入账 0=红冲冲销 | 自某时点起才启用——启用前老客户业务无流水，历史余额以官方函数/对账单为准 |
| 拜访数据 | A8 `x6_system` 库 | business_visit_statistics / plan_user_cust_task_exec |

## 客户应收/欠款标准口径（财务口径，官方函数）

查"XX 还差我们多少钱 / 应收余额 / 期末应收"时，**直接用官方应收函数取数，不要从销售/收款/核销单据手工拼**（单据口径会被"收了钱没做核销勾单"骗到：曾实测一客户大额已到账但未核销 → 单据推算仍显示大额假欠款，官方口径实为 0）。

- 期末应收余额：`SELECT qmzysk FROM TF_getKhyskQmzys('起','止',khbh)`（另含 qczysk 期初应收、qjyis 期间已收）；函数对象已加密，直接 SELECT 调用即可
- 往来对账明细：`SELECT * FROM TF_getDjmxForKhYskDtb(khbh,'起','止')`，行类型=期初(余额0) / 票前调整单(返利折让) / 销售发货单(借+) / 销售收款单(贷-) / 票前退货单(贷-) / 票前销售费用(贷-) / 期末(该行 qjje=期末应收)；勾稽=期初+销售+调整+退货+费用−收款=期末
- 日期参数传 'YYYY-MM-DD'（char）；客户范围要全史用 '2021-01-01' 起
- 可反验界面：S6"客户应收款"期末余额、客户应收款对账（逐行碰单号）

**渠道体系（kh.sszgs 渠道根）**：1=商超 / 2=母婴 / **4=流通** / 5=批发 / 6=医院 / 10=特殊。流通渠道下"流通渠道-中区/西区"等为渠道主体(khlx=1)，终端门店 khlx=2。

**仓库去向**（退货/库存口径关键）：主仓(唯一可售) / 报损仓(真损失) / 退货仓(可再销售/待处理) / 返厂仓(厂家承担)。各仓实际编码按部署查 `ck` 表。

## 客户建模七维（核心方法论）

### 维度1：活跃度（最近一次开单，四档）

旺哥口径（R 值取**全历史 MAX(xsrq)**，近90天窗口分不出 91~180 与 >180）：
```
最近一次开单距今天数：
  ≤45天    活跃
  46~90天  潜在
  91~180天 流失（预警，要挽留）
  >180天   无效（唤醒/淘汰）
```
- lastbuy CTE：`SELECT khbh, MAX(xsrq) FROM zyxsd WHERE zt<>-1 GROUP BY khbh`
- （可选细粒度：RFM 交叉 高频高额=核心 / 高频低额=潜力 / 低频高额=流失风险 / 低频低额=沉睡）

### 维度1b：新客留存（cohort，首单 → M1/M2/M3 复购）

**目的**：新客开出来后留不留得住——首单月这批客户，M1/M2/M3 是否复购，流失最高在第几环，好安排 T+1/T+7/T+14/T+30 召回。

- **新客定义**：该客户首单(`MIN(xsrq)`)落在"新客月"的流通终端。
- **留存口径**：该客户在 M1/M2/M3 月是否有销售单（zt<>-1，含未审核）。
- 输出单客户明细 + cohort 留存率；建议动作标签：首单未复购-高危 / 仅M1-流失预警 / 复购至M2-观察 / 已形成复购-健康。
- 现成 SQL：`scripts/06-new-customer-retention.sql`（② 汇总留存率、① 逐客户清单）。
- ⚠️ 至少需跨 4 个月数据才看得出 M3；样本不足时标注，勿硬下结论。

### 维度2：客户利润（核心价值指标，必含商品成本）

```
客户利润(近90天) = 销售金额 - 商品成本 - 退货全额 + 退货成本 - 报损成本 - 费用(xsfy.fyje)
  销售金额 = zyxsd.xsje（zt<>-1，近90天）
  商品成本 = zyxsdmx 数量(xssl) × 进货价(jhj)（近90天销售单）
  退货全额 = xsthd.thje（zt IN(1,2)，近90天）
  退货成本 = xsthdmx.cbje（全去向退货）
  报损成本 = 退货去向=报损仓的 cbje（真实损失）
  费用     = xsfy.fyje（已含券/满减/返现/收款调整）
```
- ⚠️ **销售商品成本必须扣减**：只算"销售-退货-费用"会把毛利 90%+ 当利润（假象）。对齐财务损益：净收入=销售-退货全额；净销售成本=商品成本-退货成本；毛利=净收入-净销售成本；利润=毛利-报损-费用
- ⚠️ **退货冲减**：退货全额冲销售、退货成本冲商品成本（货退回）；退货中报损的再作损失扣
- ⚠️ **费用已含券**：xsfy 销售费用单在销售单审核/收款调整时自动生成，已含券/满减/返现；`zdy_xsfy_yhqje` 只是用券明细分摊——**不重复扣券**；用券仅作分析列

### 维度3：真实退货（负向修正）

```
真实退货(近90天) = 退货中 去向仓=报损仓 的成本（xsthdmx.cbje）
真实退货率 = 真实退货 / 销售额
```
- 退货去向：报损仓=真实损失 / 返厂仓=厂家承担 / 退货仓=可再销售，后两者不算损失
- 全司真实退货 = 直标报损 + 退货仓移库入报损（两段式）；**客户/业务员维度移库无客户字段，只算直标报损**
- 判断依据是**去向仓库 thck**，不是退货理由文本（旧口径按退货理由已废弃）
- ⚠️ **破损仓退货"不计退货额"口径**：破损/报损仓退货单里，明细行 `thsl<>0 AND thje=0` = 不计退货额（赠品/无价退货，要从退货额中剔除）；退货额只累加 `thje>0` 的行；不计额行仍占成本(cbje)。报损单常由销售退货单审核自动生成，**按 thrq(退货日期)归期，勿用单号日期**。全司当期破损退货大头常挂某内部主体（0 元单仍占大量成本），业务员维度需单独筛 ywybh

### 维度4：回款周期（按客户维度）

```
① 时间窗：销售审核时间(zyxsd.shsj) ∈ [查询日-180天, 查询日]
② 客户已审核直营销售单
③ 每单链路：zyxsd → xsfhd.zyxsdh → xsskd_hxmx(djlx=f0) → xsskd
④ 取最早收款审核 = 首笔到账
⑤ 该单回款 = 首笔收款审核 - 销售审核；客户平均 = AVG
```
- 分档：0-3天现结 / 3-30天正常 / >60天高危欠款
- ⚠️ xsskd_hxmx 按商品行存，算单数走链路别直接 COUNT；销售单↔发货单 1:1
- **回款数据保留展示，不参与 ABC 评级**

### 维度5：返利模式（协议/普通）

- 协议客户：月销售额 2000-5000 → 2% / >5000 → 3.5% / <2000 → 普通（月末按销售额返券）
- 普通客户：每月自动领固定券包，每单抵扣；协议客户排除普通券包
- S6 无协议标记表，用数据反推：销售额>2000 且 实际返利≈销售额×比例 → 协议

### 维度6：客情关系（行为代理）

```
综合客情分 = 拜访响应 + 新品接受 + 下单稳定 + 业务配合
① 拜访响应：有效拜访率/频次（A8）
② 新品接受度：新品首销后45天内客户拿货
③ 下单稳定：下单频次波动
④ 业务配合：退货率低/回款及时
```

### 维度7：ABC 分级（按客户利润，20/50/30 纯排序）

```
客户池（见下方）按客户利润降序：
  前20% = A类（核心高价值）
  中间50% = B类（成长）
  后30% = C类（尾部/低价值）
```
- **纯利润排序，无独立降级规则**（利润已含退货成本惩罚）；利润≤0 自然落 C；回款不评级
- 沉睡/无效客户由活跃度维度独立标记
- **沉默客户/流失预警补充口径**：客户池内"近90天有销售（老客户）＋近30天既无销售也无业务员拜访" = 沉默/流失预警，要跟线激活。三层都要算在**名下客户全集**（kh.ywybh=某业务员）上，勿用全量销售单客户集。详口径与坑见 `references/QUERY-RUNBOOK.md` 沉默客户一节

## 客户池（渠道限定）

分级场景客户池 = **流通渠道终端门店**：`kh.sfyx=1 AND kh.sszgs='4' AND kh.khlx=2`。
- 排除：商超/母婴/批发/餐饮等其它渠道（sszgs≠4）；流通渠道分区主体(khlx=1)；无效客户
- ⚠️ **勿按业务员部门(bmdm)过滤客户**：会漏挂在一级部门业务员名下的客户，且 KA 连锁门店常挂流通业务员
- 无业务员客户单独标注清理；各应用可按渠道/业务员调整客户池

## 商品建模（模块二）

### 商品 ABC 分级（毛利 + 动销）

```
商品毛利贡献 = SUM(zyxsdmx.mle)（毛利额，明细行自带）
商品动销率   = 覆盖客户数(COUNT DISTINCT khbh) / 有效客户池
分级（按毛利贡献）：前20%=核心品(A) / 中60%=常规品(B) / 后20%=长尾品(C)
结合动销：高毛利+高动销=明星品 | 高毛利+低动销=潜力品 | 低毛利+高动销=走量品 | 低毛利+低动销=淘汰品
```

### 商品策略（按客户类型）

```
A类 → 推高毛利核心品；B类 → 推潜力新品；C类 → 推特价/清库品
```

### 商品质量/库存健康

```
产品退货率 = 去向报损的退货 / 销售（质量预警，按商品归）
库存健康   = 可售库存(cpkc 主仓) / 批次效期(bi_Stmt_SKU_InvBatch 仅主仓) → 临期清库
```

## 业务员建模（模块三）

五维画像：客户资产 / 业绩产出(销售额、客户利润、毛利) / 拜访效率(A8) / 回款能力 / 开发能力。
考核权重（产出为主）：拜访数量 30% / 有效产出 70%。
输出：综合评分排名 + 每日任务分派（A类流失预警 > A类 > 高潜B > 少量C唤醒≤3）。

**人效四象限 + 动销漏斗（管理视图）**：把业务员从"一个总分"变成两个能看懂的视图——四象限横轴=客户数(规模)、纵轴=户均毛利(质量)（高质高量=标杆/高量低质=靠堆数/低量高质=精耕少/低量低质=待辅导）；漏斗=覆盖客户数→活跃客户数→动销客户数（看覆盖→活跃、活跃→动销转化）。逐业务员指标与转化率见 `scripts/07-salesman-efficiency.sql`。

## 异动诊断与归因（模块四）

**目的**：指标下滑/上升时，把总量变化归到"谁贡献了多少"，再回答"为什么"——避免"这个月行情不好"式的空结论。

**数据层**（只算构成与贡献，见 `scripts/05-driver-analysis.sql`）：
- **量价拆**：销售额=销量×均价×结构，三拆出 量差贡献/价差贡献/结构差贡献（占比之和=总额差）。
- **客户/产品/业务员贡献**：逐客户/SKU/业务员 当期 vs 同期销售额差，负贡献大头即元凶。
- **头部集中度**：Top1/Top3 客户与业务员销售额占比，过高即集中度风险。

**四层归因 ↔ 数据支撑映射**（把"为什么"落到可量化的层，根因至少追问 2 层）：

| 归因层 | 用什么量化 | 落点 |
|---|---|---|
| 外部/环境 | 与同期比、剔除季节（环比必须同期口径，勿部分天数比整月） | 05 量价拆 + 判断 |
| 策略（价/促销/结构） | 量价拆 + 促销/费用（xsfy） | 05 量价拆 |
| 执行/客户-产品 | 客户贡献、产品贡献（叠加 ABC/利润档） | 05 ②③ |
| 人员 | 业务员贡献、集中度 | 05 ④⑤ |

**输出形态**：结论要"归到层 + 量化贡献占比 + 根因追 2 层 + 落到可执行动作"，动作按 `references/ACTION-FRAMEWORK.md` 排 P0/P1/P2。

## 输出物

1. **客户画像表**：khbh / 客户名 / 维护业务员 / 活跃度档 / 客户利润 / 真实退货 / 回款周期 / ABC等级 / 风险标签 / 无业务员标注
2. **客户分级报表**：ABC 分布、升降级、风险客户（无业务员/高退货/长账期/流失）
3. **AI 驱动输入**：分级结果供拜访清单/策略
4. **P0/P1/P2 行动清单**：诊断 → 可派活任务单，每条带 数据依据/根因/动作/量化目标/责任岗/时间/验证指标（见 `references/ACTION-FRAMEWORK.md`）
5. **异动归因结论**：量价拆 + 客户/产品/业务员贡献 + 头部集中度（见模块四 + `scripts/05-driver-analysis.sql`）

## 注意事项（S6 通用踩坑）

1. **客户利润必含商品成本**：漏成本把毛利 96% 当利润；退货全额冲销售、退货成本冲成本、报损再作损失
2. **费用已含券**：xsfy 已含券/满减/返现，只扣一次；用券仅分析列
3. **SQL 防笛卡尔积**：客户多销售单×退货单×费用单直接 JOIN 会行爆炸、SUM 放大 → 各指标独立 CTE 聚合再 LEFT JOIN
4. **活跃度取全历史 MAX(xsrq)**：近90天窗口分不出 91~180 与 >180
5. **客户池按渠道(sszgs=4 + khlx=2)**：勿按 bmdm 过滤；勿把 khlx=1 分区主体当客户
6. **回款周期按客户维度 + 首笔到账**，不按全体平均；xsskd_hxmx 按商品行存
7. **协议/普通客户无标记表**，用返利金额反推
8. S6 只读：只 SELECT；SQL Server 2014 无 LIMIT/FOR JSON/STRING_AGG；批次表仓库字段是 idWH（负编码，非 ckbh，需按部署实测对照）
9. **销售单实时口径（含未审核）**：zt: 1=已审核 / 0=未审核 / -1=作废，"含未审核"=`zt<>-1`。时间三口径：`xsrq`=业务/开单日、`jdrq`=手机录单、`apkwsj`=同步服务器时间——夜单可能次日才同步，按接收时间归类会算错日；次日上午审核滚动，已审/未审边界实时变但总量 zt<>-1 不变。S6 界面销售查询默认可能漏"已进装车/发货环节"的已审单（按单号搜可反验）
10. **品牌判定勿单信 b_kim_SKU.ID_Brand**（实测某品牌编码挂数百商品、品名带该品牌名的仅十几个）——品名 LIKE 与品牌编码双口径交叉后再剔除；品牌字典是 `pp` 表（wwwPPBH/wwwPPMC/ppbz），cp 表品牌字段=wwwppbh
11. **批次表字段语义**：MFG=生产日期，DTE=保质期天数（非到期日），到期=MFG+DTE；快照取 dStmt=MAX 且 q_Real>0 且 snBatch<>'.'；kID 经 b_kim_SKU.code3rd 关联商品编码
12. **统一走只读 SQL Server MCP 数据通道**：查数一律经 MCP execute_query（只读），凭据走环境变量、不散发明文、不另开直连后门；A8 库与 S6 若在同一实例，用跨库写法 `x6_system.dbo.表名` 走同一通道，勿另建独立连接。大批量明细导出用通道封装的游标脚本（MCP 文本输出约限 200 行；脚本兼容 fetchall/fetchone + 数字转 float + 空表兜底）。**计数别依赖返回的单值 COUNT**（标量经封装可能回空被当 0）——用 `SELECT DISTINCT 目标列` + 应用层 len()。同句混 `MIN(jdrq)` + 含 apkwsj 条件的标量子查询会误报 Invalid column，拆简单查询执行。连接时主库名必须精确（写错库名报 18456 登录失败，不是密码错）
13. **客户欠款/应收余额只用官方函数**（TF_getKhyskQmzys / TF_getDjmxForKhYskDtb）；"销售单未核销"≠欠款——大额收款可能到账但没做核销勾单（xsskd_hxmx 无记录），且核销表按商品行同额冗余展开
14. **bi_Stmt_ARB 是近约30天滚动窗口**的应收账龄快照，老欠款单不在表内 → 表内无行≠无欠款，勿据此下结论
15. 收款单 xsskd.zt：系统切换时点之后新单=1、之前历史归档=2（历史单大量为 2），**zt=2 不是作废**；按时间汇总收款勿 WHERE zt=1
16. **数量单位细节**：散装/小包装休闲食品（辣条/素肉卷/面筋/面包/吐司/华夫饼类等）销售与库存单位常按"袋"计，不是"件"——报库存/数量必须联表取 `cp.jldw` 带真实单位，不能默认"件"；向用户报数量一律用系统实际单位
17. **环比对比必须同期口径**："本月几天 vs 上月整月"会让环比虚报大比例下跌，结论无意义；应取**同期日数对比**。业务员销售画像侧重看**动销客户数、大客户集中度、老客户掉量**（上期有货、本期归零=流失预警），比总额更能暴露问题
18. **库存净口径与可用口径**：cpkc 按商品 SUM(cpsl) 取净库存（禁按 cpsl>0 单行筛——一正一负净零的商品会被虚算有货）；可用=净库存−近90天已开单未出库占用（cpkclsz 无对应扣减的"单+商品"行，须行级判断）；**S6 出库与审核解耦，未审单也会出库扣库存**；批次快照是 T+1 勿当实时可用
19. **利润/利润率笛卡尔积坑**：算"销售金额+商品成本"时若直接 `zyxsd JOIN zyxsdmx` 一次聚合，销售金额会按明细行数重复累加（曾实测放大近 100 倍）；必须**销售金额从单头表 zyxsd 独立聚合、商品成本(数量×进货价 jhj)从明细表 zyxsdmx 独立聚合**再合并。当日"本月累计"口径宜取"本月1号~当天"（上午推送当日可能为空）
20. **SQL IN 列表脏编码的通用坑**：`x.cod IN (拼接的长列表)` 若列表或目标表含非数字脏值，SQL Server 与 int 列比较会抛 "Conversion failed converting varchar ... to data type int"。**规避：IN 列表改用子查询** `WHERE x.cod IN (SELECT cod FROM 源表 WHERE 条件)`，别用代码拼接长 IN 列表

> 连接 S6 / 执行 / 数据源速查 / 查数示例 见 `references/S6-QUERY.md`。
> A8 拜访表结构、销售单实时/含未审核口径、品牌剔除、库存超储/可用、批次查询、沉默客户口径、大批量导出模板见 `references/QUERY-RUNBOOK.md`（2026-09 实测）。
