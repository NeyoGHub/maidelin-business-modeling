---
name: maidelin-business-modeling
description: Use when 需要查询/分析麦得邻 S6 商超系统业务数据（销售/退货/费用/库存/回款/客户/商品/业务员），或用自然语言查数、做客户价值分析与客户分层。加载后能按本 Skill 的数据字典与指标口径从 S6 取数，输出可计算画像：客户ABC分级(利润20/50/30)、活跃度四档、客户利润(含商品成本)、真实退货(报损)、回款周期、返利、客情、商品分级、业务员考核。分析麦得邻客户价值、量化客户健康度时使用。
---

# 麦得邻客户业务建模量化（商贸流通/经销商行业通用方法论）

> 本 Skill 是**平台无关**的通用知识：任何 Agent（Hermes / Claude / Cursor / OpenCode 等，支持 agentskills.io 的 SKILL.md 即可）加载后，都能用它连接 S6 数据库、按统一指标口径查询与分析。

## 用途

把商贸流通/经销商公司的客户业务量化成**可计算的模型**，用于客户 ABC 分级、客户价值评估、AI 驱动业务员（该跑哪家、精力放哪）。所有指标口径统一，分析/报告须附带指标口径说明与数据溯源（读者可登录 S6 界面反验）。

## 自然语言查 S6（通用执行流程）

任何 Agent 可用自然语言查询 S6 业务数据。执行流程：

1. **解析意图** → 指标 / 对象 / 时间范围（如"上个月销售前10的流通客户及毛利"）
2. **查口径** → 用下方各维度指标定义确定取数规则（客户利润必含成本、真实退货按报损仓、活跃度取全历史最近开单）
3. **取/拼 SQL** → 优先用 `scripts/` 模板（01 客户/02 回款/03 商品/04 业务员），不够按数据源表拼 SQL（**多指标各自 CTE 聚合再 JOIN，防笛卡尔积**）
4. **执行** → 连 S6（方式见 `references/S6-QUERY.md`；只读；SQL Server 2014 语法限制）
5. **返回** → 用数据字典解释字段；数字附指标口径说明 + 可在 S6 核对的数据源（可反验）

## 核心数据源（S6 数据字典）

| 数据 | 表 | 关键字段 |
|---|---|---|
| 直营销售单 | `zyxsd` | xsdh(YD-单号) / khbh / **xsrq** / xsje / **zt(1=已审核,-1=作废)** / shsj(审核) / ywybh |
| 销售单明细 | `zyxsdmx` | xsdh / cpbh / **xssl(数量)** / xsje / **jhj(进货价,商品成本用它)** |
| 销售发货单 | `xsfhd` | **fhdh(XF-单号)** / **zyxsdh(关联销售单)** / shsj |
| 销售收款单 | `xsskd` | skdh / khbh / skje / skrq / shsj / zt |
| 收款核销明细 | `xsskd_hxmx` | skdh / **djhm(被核销单号)** / djlx(**f0=销售**) |
| 销售退货单 | `xsthd` | khbh / thrq / thje / **thck(去向仓库)** / zt IN(1,2) |
| 退货单明细 | `xsthdmx` | thdh / cpbh / thsl / **cbje(成本金额)** / cbdj / thje |
| 销售费用单 | `xsfy` | khbh / fyje（**已含券/满减/返现**）/ fyrq / zt |
| 优惠券明细 | `zdy_xsfy_yhqje` | fydh / cpbh / yhqje（用券分析列，不重复扣） |
| 客户档案 | `kh` | khbh / dwmc / ywybh / **sfyx** / **khlx(1=渠道主体,2=终端)** / **sszgs(渠道根)** / sjkh(上级) |
| 业务员 | `ywy` | ywybh / xm(姓名) / sfywy / **bmdm** |
| 当前库存 | `cpkc` | ckbh(仓库) / cpbh / cpsl / cbdj |
| 仓库字典 | `ck` | ckbh / ckmc（7新宏达库 / 5报损仓 / 8新宏达退货仓 / 4返厂仓） |
| 批次库存 | `bi_Stmt_SKU_InvBatch` | **idWH(仓库)** / MFG / DTE / **LifeLine** / a_Real |
| 客户应收BI | `bi_Stmt_ARB` | Receivable / WO_Exp(逾期) / WO_ExpD / d_dbTerm |
| 拜访数据 | A8 `x6_system` 库 | business_visit_statistics / plan_user_cust_task_exec |

**渠道体系（kh.sszgs 渠道根）**：1=商超 / 2=母婴 / **4=流通** / 5=批发 / 6=医院 / 10=特殊。流通渠道下"流通渠道-中区/西区"等为渠道主体(khlx=1)，终端门店 khlx=2。

**仓库去向**（退货/库存口径关键）：0000000007=新宏达库(唯一可售) / 0000000005=报损仓(真损失) / 0000000008=新宏达退货仓(可再销售/待处理) / 0000000004=返厂仓(厂家承担)。

## 客户建模七维（核心方法论）

### 维度1：活跃度（最近一次开单，四档）

旺哥口径（R 值取**全历史 MAX(xsrq)**，近90天窗口分不出 91~180 与 >180）：
```
最近一次开单距今天数：
  ≤45天    活跃
  46~90天  潜在
  91~180天 流失（预警，要挽留）
  >180天   无效（唤醒/淘汰）
```
- lastbuy CTE：`SELECT khbh, MAX(xsrq) FROM zyxsd WHERE zt<>-1 GROUP BY khbh`
- （可选细粒度：RFM 交叉 高频高额=核心 / 高频低额=潜力 / 低频高额=流失风险 / 低频低额=沉睡）

### 维度2：客户利润（核心价值指标，必含商品成本）

```
客户利润(近90天) = 销售金额 - 商品成本 - 退货全额 + 退货成本 - 报损成本 - 费用(xsfy.fyje)
  销售金额 = zyxsd.xsje（zt<>-1，近90天）
  商品成本 = zyxsdmx 数量(xssl) × 进货价(jhj)（近90天销售单）
  退货全额 = xsthd.thje（zt IN(1,2)，近90天）
  退货成本 = xsthdmx.cbje（全去向退货）
  报损成本 = 退货去向=报损仓(5) 的 cbje（真实损失）
  费用     = xsfy.fyje（已含券/满减/返现/收款调整）
```
- ⚠️ **销售商品成本必须扣减**：只算"销售-退货-费用"会把毛利 90%+ 当利润（假象）。对齐财务损益：净收入=销售-退货全额；净销售成本=商品成本-退货成本；毛利=净收入-净销售成本；利润=毛利-报损-费用
- ⚠️ **退货冲减**：退货全额冲销售、退货成本冲商品成本（货退回）；退货中报损的再作损失扣
- ⚠️ **费用已含券**：xsfy 销售费用单在销售单审核/收款调整时自动生成，已含券/满减/返现；`zdy_xsfy_yhqje` 只是用券明细分摊——**不重复扣券**；用券仅作分析列

### 维度3：真实退货（负向修正）

```
真实退货(近90天) = 退货中 去向仓=报损仓(5) 的成本（xsthdmx.cbje）
真实退货率 = 真实退货 / 销售额
```
- 退货去向：报损仓(5)=真实损失 / 返厂仓(4)=厂家承担 / 新宏达退货仓(8)=可再销售，后两者不算损失
- 全司真实退货 = 直标报损 + 退货仓8移库入报损5（两段式）；**客户/业务员维度移库无客户字段，只算直标报损**
- 判断依据是**去向仓库 thck**，不是退货理由文本（旧口径按 thly 已废弃）

### 维度4：回款周期（按客户维度）

```
① 时间窗：销售审核时间(zyxsd.shsj) ∈ [查询日-180天, 查询日]
② 客户已审核直营销售单
③ 每单链路：zyxsd → xsfhd.zyxsdh → xsskd_hxmx(djlx=f0) → xsskd
④ 取最早收款审核 = 首笔到账
⑤ 该单回款 = 首笔收款审核 - 销售审核；客户平均 = AVG
```
- 分档：0-3天现结 / 3-30天正常 / >60天高危欠款
- ⚠️ xsskd_hxmx 按商品行存，算单数走链路别直接 COUNT；销售单↔发货单 1:1
- **回款数据保留展示，不参与 ABC 评级**

### 维度5：返利模式（协议/普通）

- 协议客户：月销售额 2000-5000 → 2% / >5000 → 3.5% / <2000 → 普通（月末按销售额返券）
- 普通客户：每月自动领固定券包（满198减2 等），每单抵扣；协议客户排除普通券包
- S6 无协议标记表，用数据反推：销售额>2000 且 实际返利≈销售额×比例 → 协议

### 维度6：客情关系（行为代理）

```
综合客情分 = 拜访响应 + 新品接受 + 下单稳定 + 业务配合
① 拜访响应：有效拜访率/频次（A8）
② 新品接受度：新品首销后45天内客户拿货
③ 下单稳定：下单频次波动
④ 业务配合：退货率低/回款及时
```

### 维度7：ABC 分级（按客户利润，20/50/30 纯排序）

```
客户池（见下方）按客户利润降序：
  前20% = A类（核心高价值）
  中间50% = B类（成长）
  后30% = C类（尾部/低价值）
```
- **纯利润排序，无独立降级规则**（利润已含退货成本惩罚）；利润≤0 自然落 C；回款不评级
- 沉睡/无效客户由活跃度维度独立标记

## 客户池（渠道限定）

旺哥分级场景客户池 = **流通渠道终端门店**：`kh.sfyx=1 AND kh.sszgs='4' AND kh.khlx=2`（约 1760 家，与 S6 界面一致）。
- 排除：商超/母婴/批发/餐饮等其它渠道（sszgs≠4）；流通渠道分区主体(khlx=1)；无效客户
- ⚠️ **勿按业务员部门(bmdm)过滤客户**：会漏 bmdm='01' 的流通业务员(张玉霞等)管的客户，且 KA 连锁门店常挂流通业务员
- 无业务员客户单独标注清理；各应用可按渠道/业务员调整客户池

## 商品建模（模块二）

### 商品 ABC 分级（毛利 + 动销）

```
商品毛利贡献 = SUM(zyxsdmx.mle)（毛利额，明细行自带）
商品动销率   = 覆盖客户数(COUNT DISTINCT khbh) / 有效客户池
分级（按毛利贡献）：前20%=核心品(A) / 中60%=常规品(B) / 后20%=长尾品(C)
结合动销：高毛利+高动销=明星品 | 高毛利+低动销=潜力品 | 低毛利+高动销=走量品 | 低毛利+低动销=淘汰品
```

### 商品策略（按客户类型）

```
A类 → 推高毛利核心品；B类 → 推潜力新品；C类 → 推特价/清库品
```

### 商品质量/库存健康

```
产品退货率 = 去向报损的退货 / 销售（质量预警，按商品归）
库存健康   = 可售库存(cpkc 新宏达库7) / 批次效期(bi_Stmt_SKU_InvBatch 仅7仓) → 临期清库
```

## 业务员建模（模块三）

五维画像：客户资产 / 业绩产出(销售额、客户利润、毛利) / 拜访效率(A8) / 回款能力 / 开发能力。
考核权重（产出为主）：拜访数量 30% / 有效产出 70%。
输出：综合评分排名 + 每日任务分派（A类流失预警 > A类 > 高潜B > 少量C唤醒≤3）。

## 输出物

1. **客户画像表**：khbh / 客户名 / 维护业务员 / 活跃度档 / 客户利润 / 真实退货 / 回款周期 / ABC等级 / 风险标签 / 无业务员标注
2. **客户分级报表**：ABC 分布、升降级、风险客户（无业务员/高退货/长账期/流失）
3. **AI 驱动输入**：分级结果供拜访清单/策略

## 注意事项（S6 通用踩坑）

1. **客户利润必含商品成本**：漏成本把毛利 96% 当利润；退货全额冲销售、退货成本冲成本、报损再作损失
2. **费用已含券**：xsfy 已含券/满减/返现，只扣一次；用券仅分析列
3. **SQL 防笛卡尔积**：客户多销售单×退货单×费用单直接 JOIN 会行爆炸、SUM 放大 → 各指标独立 CTE 聚合再 LEFT JOIN
4. **活跃度取全历史 MAX(xsrq)**：近90天窗口分不出 91~180 与 >180
5. **客户池按渠道(sszgs=4 + khlx=2)**：勿按 bmdm 过滤；勿把 khlx=1 分区主体当客户
6. **回款周期按客户维度 + 首笔到账**，不按全体平均；xsskd_hxmx 按商品行存
7. **协议/普通客户无标记表**，用返利金额反推
8. S6 只读：只 SELECT；SQL Server 2014 无 LIMIT/FOR JSON/STRING_AGG；批次表仓库字段是 idWH（新宏达库=-32762，非 ckbh）

> 连接 S6 / 执行 / 数据源速查 / 查数示例 见 `references/S6-QUERY.md`。
